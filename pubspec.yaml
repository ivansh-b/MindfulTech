name: health_flutter_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dashed_circular_progress_bar: ^0.0.4
  app_usage: ^2.1.1
  firebase_core:
  firebase_auth:
  cloud_firestore:
  intl:
  audioplayers:

  # workmanager:
  # flutter_local_notifications:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audios/
